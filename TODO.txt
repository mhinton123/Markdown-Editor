


-- add light theme and get theme switch working

-- put a character limit on the text area

-- add cursor for all buttons

-- add hover state for small icons

-- find out all markdown rules

-- fix name input to > 0 && < 25 characters

-- alert feedback when savechanges btn clicked

-- sort document list in sidebar by date & time



-- add delete feature
    -- when the bin is clicked
        --  -- get name of file from #filename.dataset.name (targetFileName)
        -- update file name in modal message
        -- modal pops up asking if they are sure
        
-- if yes button is clicked
    -- get name of file from #filename.dataset.name (targetFileName)
    -- pull local storage 
    -- filter file arr => file.name != targetFileName (filteredArr)
    -- push filteredArr to local storage (stringify)
    -- renderFile(last edited file)

-- if no button is clicked
    -- hide modal
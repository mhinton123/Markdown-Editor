


-- add light theme and get theme switch working

-- put a character limit on the text area

-- add cursor for all buttons

-- add hover state for small icons



-- Get intereter working!!

    -- get input from text area
    -- loop through each line and store each line in an array

    -- loop through array and check the following

        -- starts with '#' -> h1  do this for all 6 Headings
        -- starts with a number followed by a '.' -> ol
        -- starts with '-' -> ul
        -- starts with '>' -> blockquote (look into inline links for this)
        -- starts with '```' -> all subsequent lines are 'code' while start != '```'
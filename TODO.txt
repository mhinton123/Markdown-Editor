


-- add light theme and get theme switch working

-- put a character limit on the text area

-- add cursor for all buttons

-- add hover state for small icons

-- find out all markdown rules

-- fix name input to > 0 && < 25 characters

-- alert feedback when savechanges btn clicked



-- whenever New document btn is clicked
    -- #file-name.value & #file-name.dataset.name = new-document.md
    -- #markdown-input is cleared
    -- a new file is pushed to Local Storage
        -- Pull Local Storage (parse)
        -- push the following
            {
                name: "new-document.md",
                createdAt: new Date().toISOString,
                content: ''
            }
        -- push amended data arr to Local storage
        -- renderFilesInfoToSidebar()